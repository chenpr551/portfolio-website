export const BLOCK_COLORS = [
  "bg-accent-orange",
  "bg-accent-lime",
  "bg-accent-blue",
  "bg-accent-yellow",
];

/** Same four-color sequence as BLOCK_COLORS, as CSS custom-property references
 * for use in inline styles (e.g. the cursor's hover accent, row particles). */
export const CATEGORY_ACCENTS = [
  "var(--accent-orange)",
  "var(--accent-lime)",
  "var(--accent-blue)",
  "var(--accent-yellow)",
];
